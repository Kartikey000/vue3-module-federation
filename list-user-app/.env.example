# List User App Environment Variables

# Host App URL (for consuming shared store)
HOST_APP_URL=http://localhost:5000

# This app's own URL (required for production builds with Module Federation)
# For production, set this to your Vercel deployment URL
APP_URL=http://localhost:5001
